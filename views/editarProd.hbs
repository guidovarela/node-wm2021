
<div class="container">

    {{!-- <div class="">
        {{#each getListColumn}}
            {{fieldName}}
        {{/each}}
    </div> --}}

    <table class="table table-striped table-hover">
        <thead>
            <tr>
            {{!-- {{#each getListColumn}}
                <th scope="col">{{fieldName}}</th>
            {{/each}} --}}
            <th scope="col">id</th>
            <th scope="col">imagen</th>
            <th scope="col">nombre</th>
            <th scope="col" class="d-lg-block d-md-none d-none">despripcion</th>
            <th scope="col">precio</th>
            <th scope="col">categoria</th>
            <th scope="col">editar</th>
            <th scope="col">eliminar</th>
            </tr>
        </thead>
        <tbody>
            {{#each listProd}}
            <tr>
                <td>{{id}}</td>
                <td><img style="width:80px" src="/images/{{imagen}}" alt=""></td>
                <td>{{nombre}}</td>
                <td class="d-lg-block d-md-none d-none">{{descripcion}}</td>
                <td>{{precio}}</td>
                <td>{{categoria}}</td>
                <td>
                    <a class="btn btn-primary" href="/edit-prod/{{id}}">
                        <i class="fas fa-pencil-alt"></i>
                    </a>
                </td>
                <td>
                    <a class="btn btn-primary" href="/delete-prod/{{id}}">
                        <i class="fas fa-trash-alt"></i>
                    </a>
                </td>
            </tr>
            {{/each}}
        </tbody>
        </table>

</div>